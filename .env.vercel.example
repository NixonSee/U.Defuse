# === Database Configuration (Aiven MySQL) ===
DB_HOST=your-mysql-instance.aivencloud.com
DB_PORT=12345
DB_USER=avnadmin
DB_PASSWORD=your-aiven-password-here
DB_NAME=defaultdb
DB_SSL=true

# === JWT Secret ===
# Generate a secure random string (minimum 32 characters)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long-change-this

# === Node Environment ===
NODE_ENV=production

# === Optional: Custom Backend URL ===
# Only needed if deploying backend separately (e.g., Railway)
# VITE_API_URL=https://your-backend.railway.app
# VITE_SOCKET_URL=https://your-backend.railway.app

# === CORS Origins (Optional) ===
# Add your Vercel deployment URLs for additional CORS validation
# CLIENT_ORIGIN=https://your-app.vercel.app
# CLIENT_ORIGIN2=https://your-app-git-main.vercel.app
